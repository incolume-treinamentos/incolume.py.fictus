#!/bin/bash

BRANCH=$(git rev-parse --abbrev-ref HEAD)
REGEX="^(dev|tags|(enhancement/epoch#[0-9]{10,}))$"

if ! [[ $BRANCH =~ $REGEX ]]; then
  echo "Your commit was rejected due to branching name"
  echo "Please rename your branch with 'enhancement/epoch#timestamp' syntax"
  exit 1
fi
